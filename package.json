{
  "name": "skillswap",
  "version": "1.0.0",
  "description": "SkillSwap - AI-powered peer-to-peer learning platform",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\" --names \"backend,frontend\" --prefix-colors \"bgBlue.bold,bgMagenta.bold\"",
    "dev:backend": "npm run dev --workspace=backend",
    "dev:frontend": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=frontend",
    "start": "npm start --workspace=backend",
    "seed": "npm run seed --workspace=backend",
    "clean": "rm -rf node_modules frontend/node_modules backend/node_modules",
    "fresh-install": "npm run clean && npm install",
    "info": "echo '\n✨ SkillSwap Workspace Info\n' && echo '📦 Packages:' && echo \"  Root: $(ls node_modules 2>/dev/null | wc -l | xargs) packages\" && echo \"  Frontend: $(ls frontend/node_modules 2>/dev/null | wc -l | xargs) packages\" && echo \"  Backend: $(ls backend/node_modules 2>/dev/null | wc -l | xargs) packages\" && echo '\n🚀 Run: npm run dev' && echo '📚 Docs: See SETUP_COMPLETE.md\n'"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "talkjs": "^0.42.1"
  }
}
